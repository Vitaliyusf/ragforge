/**
 * Settings route. Previously a shell-less page that rendered ConfigTab on its
 * own — no header, no navigation, no way back. It now resolves to the Settings
 * destination inside the persistent shell (see routeTabs.tabForPathname).
 */
export default function Settings() {
  return null
}
