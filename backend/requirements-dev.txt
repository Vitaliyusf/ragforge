# Local development tooling: static analysis plus the canonical test toolchain.
# Test-runner pins live in requirements-test.txt so CI can install them alone.
-r requirements-test.txt

# Type checking
mypy==1.10.1
types-pyyaml
types-requests

# Linting and formatting
ruff==0.4.9
black==24.4.2
