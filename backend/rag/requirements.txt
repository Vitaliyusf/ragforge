# Shared runtime pins live in docker/requirements-base.txt.
#
# numpy is pinned in the three services that use it — embedding, rag and
# vector_db — rather than in the base file: putting it there added ~60 MB to
# every other image for nothing. The three pins move together.
numpy==1.26.4
python-socketio[asyncio]==5.16.4
aiohttp==3.14.3
# See backend/files/requirements.txt for why langgraph is not in the base file.
langgraph==1.2.11
# CrossEncoder is the production learned-reranker adapter. Keep the same 5.x
# line as embedding so the repository has one transformers/torch dependency set.
sentence-transformers==5.7.0
