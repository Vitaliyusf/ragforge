# Shared runtime pins live in docker/requirements-base.txt.
#
# langgraph is pinned here and in backend/rag/requirements.txt rather than in
# the base file: only these two services own graphs, and pushing langchain-core
# into every other image for their sake is not worth it. The two pins move
# together.
#
# 1.x rather than the 0.2 line: 0.2.74 carries PYSEC-2026-83 and
# PYSEC-2026-2194, and 1.2.11 is also what langchain 1.3 requires, so the
# repository holds one LangGraph major.
langgraph==1.2.11
