# Shared runtime pins live in docker/requirements-base.txt.
#
# numpy is pinned in the three services that use it — embedding, rag and
# vector_db — rather than in the base file: putting it there added ~60 MB to
# every other image for nothing. The three pins move together.
numpy==1.26.4

# sentence-transformers is the only embedding implementation. It stays on the
# 5.x line: 6.x requires transformers 5.x and huggingface-hub 1.x, which is a
# separate ML runtime migration with no benefit for `SentenceTransformer.encode`.
sentence-transformers==5.7.0
pymupdf==1.28.2
python-docx==1.2.0
docx2txt==0.9
