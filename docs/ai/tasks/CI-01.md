# CI-01 — CI build, security and RAG regression gates

**Branch:** `ci/harden-quality-gates`

## Goal
Catch frontend build, supply-chain and deterministic retrieval regressions automatically.

## Problem
Unit/lint CI alone misses production build and dependency/security/retrieval regressions.

## Primary scope
- `.github/workflows`
- `small committed deterministic eval fixtures where feasible`

## Required behavior
- Frontend `npm run build`.
- Reasonable dependency/secret/static/container/SBOM checks.
- Tiny deterministic retrieval regression harness without GPU/network fragility.

## Acceptance
- No fake quality threshold; baseline is only enforced after a real reproducible value exists.

## Task rules
- Follow root and scoped AGENTS.md/CLAUDE.md.
- Inspect current code before editing; current implementation wins over stale assumptions.
- Keep this branch limited to this task.
- Run focused tests, then broader affected checks.
- Do not commit or push; return a recommended Conventional Commit message.
