# CI-04 — Frontend build + staged security scans

## Execution location

**RUN BY:** Codex locally on the user's machine.

**REPOSITORY ROOT:**
```powershell
cd "C:/Users/vital/Desktop/AgentAPP/ragapp-public"
```

## Global rules

- Do not reset, stash, clean, revert, or switch branches.
- Do not commit or push unless explicitly requested.
- Inspect current code before editing.
- TEST WHAT CHANGED. LET CI TEST THE REPOSITORY.
- Use the canonical runtime/tooling from `docs/ai/RUNTIME_CONTRACT.md`.
- Do not weaken tests, mypy, Ruff, auth, security, or benchmark gates just to make them pass.
- Stop on any new unrelated failure and report it.


## Files to inspect
- `.github/workflows/ci.yml`
- `frontend/package.json`
- Python requirements files
- existing repo guardrails

## Required change
Add:
- frontend `npm run build`
- Python dependency vulnerability scan
- JS dependency vulnerability scan
- optional container scan only if CI time remains reasonable
Start noisy scans report-only if needed; do not hide findings with broad ignores.

## LOCAL FRONTEND TEST — RUN BY CODEX
```powershell
cd "C:/Users/vital/Desktop/AgentAPP/ragapp-public/frontend"
npm test
npm run build
```

## LOCAL WORKFLOW REVIEW — RUN BY CODEX
Validate YAML/diff and existing job matrix.

## REMOTE TEST — RUN BY GITHUB ACTIONS
After user-authorized push/PR:
- full CI
- frontend build
- security scans

## DO NOT RUN
Smoke30 / Full240.

## STOP CONDITION
CI catches frontend production-build failure and produces actionable dependency/security findings.
